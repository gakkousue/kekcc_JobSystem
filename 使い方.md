# JobSystem 使い方ガイド

このリポジトリは、様々なスクリプトや物理解析処理（フォーマット変換、Marlin等の並列実行）を一貫した仕組みで実行・管理・ロギングするための「ジョブフレームワーク」です。

最も一般的な利用方法は、システムに含まれる**コンソールコマンド `batch_run` を使い、JSONファイルで定義したパラメータに従って複数のジョブを連続実行（バッチ実行）すること**です。

---

## `batch_run` を使った実行手順（基本）

### 1. コマンドの構文
環境構築（`pip install -e .`）が完了していれば、ターミナルから直接 `batch_run` コマンドを呼び出すことができます。

```bash
# 基本的な使い方 (実行した場所に全体のバッチログを出力)
batch_run <設定JSONのパス>

# 全体のログを出力する場所(outputdir)を明示的に指定する場合
batch_run <設定JSONのパス> <全体ログ出力先ディレクトリ>
```

※モジュール経由で呼び出すことも可能です。
```bash
python -m job_framework.batch_run <設定JSONのパス>
```

### 2. JSONファイル (`config.json`) の書き方
`batch_run` に渡すJSONファイルで、「何を」「共通設定は何か」「各ジョブ特有の引数は何か」を決定します。

#### ▼ JSONの骨格と基本ルール
JSONは必ず以下の4つのブロック（キー）から構成されます。

1. **`target_module`** または **`target_script`**
   - 実行するべきPythonコードが「どこ」にあるかを指定します。
   - `target_module` はインストール済みパッケージのインポートパス（例: `job_modules.root_to_h5.root_to_h5_LSFJob`）。
   - `target_script` は独自の外部Pythonスクリプトファイルへの直接の相対/絶対パス（例: `./scripts/my_job.py`）です。
2. **`target_class`**
   - 実行するPythonコードの中に定義されているクラス名です（例: `RootToH5LSFJob`, `MarlinJob`など）。
3. **`common_settings`**
   - すべてのジョブで共通して使うパラメータセットを辞書形式で記述します。各ジョブクラス固有のオプション（`--queue`, `-b`, `--compression-level` など）のデフォルト値として機能します。
4. **`jobs`**
   - 個別のジョブ情報をリスト形式で羅列します（1要素が1ジョブ）。入力ファイルなどの変化する値だけをここに書きます。

#### ▼ 記述例
```json
{
  "target_module": "job_modules.run_marlin.run_marlin",
  "target_class": "MarlinJob",
  "common_settings": {
    "base_output_dir": "/path/to/base/results/dir",
    "--queue": "l",
    "--marlin-output-param-name": "MyLCIOOutputProcessor.LCIOOutputFile"
  },
  "jobs": [
    {
      "comment": "サンプルデータ1の処理",
      "outputdir": "sample1",
      "listfile": "lists/sample1.list",
      "count": 10,
      "xml": "settings/steer1.xml"
    },
    {
      "comment": "サンプルデータ2の処理",
      "outputdir": "sample2",
      "listfile": "lists/sample2.list",
      "count": -1,
      "xml": "settings/steer2.xml"
    }
  ]
}
```

### 3. このJSONを実行するとどう動くか（内部挙動）

上記の JSON を `batch_run my_config.json` で実行した際、システムは以下のプロセスを自動で行います。

1. **クラスのロード**: 指定したパッケージから `MarlinJob` を動的にインポートします。
2. **事前検証 (Phase 1)**: `common_settings` の値と `jobs` リストの中身を結合し、「この引数の組み合わせでコマンドラインオプションとして正しく解釈できるか？」を事前に全ジョブ分リハーサルします。必須引数不足等があればここで安全に停止します。
3. **連続実行 (Phase 2)**:
   - 全体を統括するログとして、実行場所に `batch_run.log` を作成します。
   - 1番目のジョブを開始します。このとき、自動で個別のログファイル `sample1/run_marlin.log` が開かれ、ターミナル出力はターミナルにも表示されつつ、そのまま個別ファイル＆全体ログファイルにも書き込まれます（二重ロギング）。ターミナルの出力は親と子を区別しやすいようインデントされます。
   - 1番目が終わると、続けて2番目のジョブを開始します。全てのエラーはスタックトレースを含めログに格納され、次へ進みます。

---

## 独自のパラメータと各種マニュアルの参照

JSONの `common_settings` や `jobs` に書くことができるパラメーターキー (`--queue`, `xml`, `listfile`, `count` など) は、**`target_class` で指定したジョブクラスが実装している引数に完全に依存**します。

使用可能なパラメーターや、それぞれのモジュールが内部でどう動作するかについての詳細は、以下のマニュアルを参照してください。

### ドキュメント（マニュアル）一覧

- **各モジュール自体の使い方や機能、固有の引数について:**
  👉 [`job_modules/src/job_modules/Module_Execution_Manual.md`](job_modules/src/job_modules/Module_Execution_Manual.md)

- **フレームワーク全体の仕組みと実行処理フロー:**
  👉 [`job_framework/src/job_framework/Execution_Flow_Manual.md`](job_framework/src/job_framework/Execution_Flow_Manual.md)

- **基底クラスの設計パラダイム・継承API（開発者向け）:**
  👉 [`job_framework/src/job_framework/Class_Reference_Manual.md`](job_framework/src/job_framework/Class_Reference_Manual.md)
